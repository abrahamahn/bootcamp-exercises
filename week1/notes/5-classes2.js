class Player {

    constructor(name, color) {
        this.name = name;
        this.color = color;
        this.position = 0;
        this.cash = 1000;
    }
}

moveBy() {
    let dice = 1 + Math.floor(6 * Math.random());
    this.position = (this.position)
}